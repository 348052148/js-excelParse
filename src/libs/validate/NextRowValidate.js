class NextRowValidate {
    
    constructor (i,rowData){
        this.i = i;
        this.rowData = rowData;
    }

    validate(row){
        if(this.rowData.hasOwnProperty[i+1]){
            this.rowData[i+1]
        }
    }
}